import React,{useEffect,useState,useRef} from 'react';
import {createRoot} from 'react-dom/client';
import {Dialog,DropdownMenu} from 'radix-ui';
import {Button} from './components/ui/button';
import {Input} from './components/ui/input';
import {Icon} from './Icon';
import {saveState,putFile,getFile,hasPendingWrites} from './storage';
import {summarize,statuses,csvCell,validateFile,transition} from './domain.mjs';
import {useAI,recoverAnalysis,AIBar,AIReview,AIBadge} from './ai';
import {formatPeriod as month,currentPeriod,documentName} from './format.mjs';
import {Runtime,ErrorBoundary} from './Runtime';
import {BackupPanel} from './BackupPanel';
import {eligible} from './ai-domain.mjs';
import {migrateWorkspace,companyHistory,profileError,companyRequirements} from './workspace.mjs';

const tabs=[['documents','Hujjatlar','document'],['history','Tarix','clock'],['archive','Doimiy hujjatlar','folder'],['info','Ma’lumotlar','company']];

const initials=(name:string)=>name.split(' ').filter(Boolean).slice(0,2).map(x=>x[0]).join('').toUpperCase();
function Badge({status,children}:any){return <span className={`badge ${status==='accepted'?'solid':''}`}><Icon size={14} name={status==='accepted'?'check':status==='review_required'?'search':status==='correction_requested'?'alert':'clock'}/>{children||statuses[status]}</span>}
function Empty({title,text,action}:any){return <div className="empty"><div className="empty-icon"><Icon name="folder" size={27}/></div><h3>{title}</h3><p>{text}</p>{action}</div>}
function Modal({open,onClose,title,description,children,wide=false}:any){return <Dialog.Root open={open} onOpenChange={v=>!v&&onClose()}><Dialog.Portal><Dialog.Overlay className="modal-overlay"/><Dialog.Content className={`modal ${wide?'wide':''}`}><header><div><Dialog.Title>{title}</Dialog.Title><Dialog.Description>{description}</Dialog.Description></div><Dialog.Close asChild><Button variant="ghost" size="icon" aria-label="Yopish"><Icon name="close"/></Button></Dialog.Close></header>{children}</Dialog.Content></Dialog.Portal></Dialog.Root>}
function Brand(){return <a href="#companies" className="brand" aria-label="Hisobkor.uz bosh sahifasi"><img src="/brand-h.png" alt=""/><strong>hisobkor<span>.uz</span></strong></a>}
function App({saved,session,logout}:any){
 const [initial]=useState(()=>migrateWorkspace(saved));
 const [companies,setCompanies]=useState<any[]>(initial.companies);
 const [docs,setDocs]=useState<any[]>(()=>initial.docs.map(recoverAnalysis));
 const [activity,setActivity]=useState<any[]>(initial.activity);
 const [profile,setProfile]=useState<any>(initial.profile);
 const [closed,setClosed]=useState<string[]>(initial.closed);
 const [route,setRoute]=useState(location.hash.slice(1)||'companies');
 const [period,setPeriod]=useState(currentPeriod());
 const [query,setQuery]=useState('');
 const [filter,setFilter]=useState('all');
 const [modal,setModal]=useState<any>(null);
 const [error,setError]=useState('');
 const [toast,setToast]=useState('');
 const [busy,setBusy]=useState(false);
 const [preview,setPreview]=useState('');
 const [mobileMenu,setMobileMenu]=useState(false);
 const [showExamples,setShowExamples]=useState(false);
 const [savedError,setSavedError]=useState('');
 const [saveStatus,setSaveStatus]=useState('saved');
 const [showSampleDocs,setShowSampleDocs]=useState(false);
 const [page,setPage]=useState(1);
 const pendingSave=useRef(false);
 const [aiAuto,setAiAuto]=useState(initial.aiAuto!==false);
 useEffect(()=>{const update=()=>{setRoute(location.hash.slice(1)||'companies');setQuery('');setFilter('all');setMobileMenu(false)};addEventListener('hashchange',update);return()=>removeEventListener('hashchange',update)},[]);
 const workspaceState={...initial,companies,docs,activity,closed,profile,aiAuto,workspace:profile?.workspace||initial.workspace};
 useEffect(()=>{let current=true;pendingSave.current=true;setSaveStatus('saving');const t=setTimeout(()=>{saveState(workspaceState).then(()=>{if(current){pendingSave.current=false;setSavedError('');setSaveStatus('saved')}}).catch(e=>{if(current){setSavedError(e.message||'Ma’lumotlar saqlanmadi.');setSaveStatus('error')}})},200);return()=>{current=false;clearTimeout(t)}},[companies,docs,activity,closed,profile,aiAuto]);
 useEffect(()=>{const warn=(e:BeforeUnloadEvent)=>{if(pendingSave.current||hasPendingWrites()){e.preventDefault();e.returnValue=''}};addEventListener('beforeunload',warn);return()=>removeEventListener('beforeunload',warn)},[]);
 useEffect(()=>{if(!toast)return;const t=setTimeout(()=>setToast(''),4500);return()=>clearTimeout(t)},[toast]);
 useEffect(()=>()=>{if(preview)URL.revokeObjectURL(preview)},[preview]);
 const parts=route.split('/');
 const company=parts[0]==='company'?companies.find(c=>c.id===parts[1]):null;
 const tab=tabs.some(t=>t[0]===parts[2])?parts[2]:'documents';
 useEffect(()=>setPage(1),[query,filter,period,company?.id,tab,showSampleDocs]);
 const mine=companies.filter(c=>!c.isDemo);
 const examples=companies.filter(c=>c.isDemo);
 const go=(r:string)=>{location.hash=r;setQuery('');setFilter('all');setMobileMenu(false)};
 const open=(value:any)=>{setMobileMenu(false);setModal(value);setError('');setPreview('')};
 const event=(title:string,c:any,detail:string,type='document')=>setActivity(a=>[{id:crypto.randomUUID(),title,companyId:c.id,detail:`${c.name} · ${detail}`,type,at:new Date().toISOString(),time:new Date().toLocaleTimeString('uz-UZ',{hour:'2-digit',minute:'2-digit'}),actor:profile?.fullName||'Buxgalter'},...a]);
 const ai=useAI({docs,setDocs,companies,onEvent:event,auto:aiAuto,enabled:!!profile,canAnalyze:()=>saveStatus==='saved'&&!pendingSave.current&&!hasPendingWrites()});
 const activeDoc=modal?.doc?(docs.find(d=>d.id===modal.doc.id)||modal.doc):null;
 const reopen=(id:string)=>setClosed(a=>a.filter(x=>x!==`${id}:${period}`));
 const current=company?companyRequirements(docs,company.id,period).filter(d=>showSampleDocs||!d.demo):[];
 const stats=summarize(current);
 const documentList=company?(tab==='archive'?docs.filter(d=>d.company===company.id&&d.scope==='permanent'&&(showSampleDocs||!d.demo)):current):[];
 const visible=documentList.filter(d=>(filter==='all'||(filter==='outstanding'?['missing','correction_requested'].includes(d.status):d.status===filter))&&`${d.title} ${d.fileName}`.toLowerCase().includes(query.toLowerCase()));
 function saveProfile(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const value={fullName:String(f.get('fullName')).trim(),phone:String(f.get('phone')).trim(),email:String(f.get('email')||'').trim(),workspace:String(f.get('workspace')||'').trim()};const message=profileError(value);if(message){setError(message);return;}setProfile(value);setError('');setModal(null);go('companies');setToast('Profil saqlandi. Endi kompaniyalaringiz bilan ishlashingiz mumkin.');}
 function ProfileForm({editing=false}:any){const errorId=error?'profile-error':undefined;return <form onSubmit={saveProfile} className="profile-form" aria-describedby={errorId}><label>Ism va familiya <span aria-hidden="true">*</span><Input name="fullName" autoComplete="name" required maxLength={100} placeholder="Ism va familiyangiz" defaultValue={profile?.fullName||''} aria-invalid={!!error} aria-describedby={errorId}/></label><label>Telefon raqami <span aria-hidden="true">*</span><Input name="phone" type="tel" autoComplete="tel" required maxLength={20} placeholder="+998 90 123 45 67" defaultValue={profile?.phone||''} aria-invalid={!!error} aria-describedby={errorId}/></label><label>Elektron pochta <span className="optional">Ixtiyoriy</span><Input name="email" type="email" autoComplete="email" placeholder="siz@pochta.uz" defaultValue={profile?.email||''}/></label><label>Buxgalteriya xizmatingiz nomi <span className="optional">Ixtiyoriy</span><Input name="workspace" placeholder="Masalan, Hisob Servis" maxLength={80} defaultValue={profile?.workspace||''}/></label>{error&&<p id="profile-error" className="form-error" role="alert">{error}</p>}<Button type="submit" className="full-width">{editing?'Saqlash':'Davom etish'}<Icon name="arrow"/></Button><p className="form-footnote"><Icon name="shield" size={16}/>{session.storage==='server'?'Ma’lumotlar serverdagi ish joyingizda saqlanadi.':'Ma’lumotlar shu brauzerda saqlanadi.'}</p></form>}
 function addCompany(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const name=String(f.get('name')).trim();const stir=String(f.get('stir')||'').trim();if(!name){setError('Kompaniya nomini kiriting.');return;}if(stir&&companies.some(c=>c.stir===stir)){setError('Bu STIR bilan kompaniya avval qo‘shilgan.');return;}const c={id:crypto.randomUUID(),name,stir,legal:String(f.get('legal')),contact:String(f.get('contact')||'').trim(),phone:String(f.get('phone')||'').trim(),owner:profile.fullName,isDemo:false,createdAt:new Date().toISOString()};setCompanies(a=>[...a,c]);event('Kompaniya qo‘shildi',c,profile.fullName,'company');setModal(null);go(`company/${c.id}/documents`);setToast('Kompaniya qo‘shildi. Birinchi hujjatni yuklashingiz mumkin.');}
 const pageSize=15;const totalPages=Math.max(1,Math.ceil(visible.length/pageSize));const safePage=Math.min(page,totalPages);const pageDocs=visible.slice((safePage-1)*pageSize,safePage*pageSize);
 const realDocs=docs.filter(d=>!d.demo);
 async function restoreWorkspace(value:any){await saveState(value);location.reload()}
 async function retrySave(){setSaveStatus('saving');try{await saveState(workspaceState);pendingSave.current=false;setSavedError('');setSaveStatus('saved')}catch(e:any){setSavedError(e.message);setSaveStatus('error')}}
 async function signOut(){try{await saveState(workspaceState);pendingSave.current=false;await logout()}catch(e:any){setSavedError(e.message)}}
 async function showDocument(d:any){if(d.status==='missing'){open({type:'upload',target:d});return;}open({type:'document',doc:d});if(!d.demo){try{const file=await getFile(d.fileKey||d.id,d.fileName);if(!file)throw Error();if(/\.(pdf|png|jpe?g)$/i.test(file.name))setPreview(URL.createObjectURL(file))}catch{setError('Asl fayl topilmadi. Qayta yuklab ko‘ring.')}}}
 async function upload(e:any,files:File[]){e.preventDefault();if(busy)return;const f=new FormData(e.currentTarget);if(!files.length){setError('Yuklash uchun fayl tanlang.');return;}if(files.length>(modal.target?1:20)){setError(modal.target?'Ushbu talab uchun bitta fayl tanlang.':'Bir martada 20 tagacha fayl yuklang.');return;}for(const file of files){const msg=validateFile(file);if(msg){setError(`${file.name}: ${msg}`);return;}}setBusy(true);const owner=company;try{const items=[];for(const file of files){const old=modal.target;const fileKey=crypto.randomUUID();await putFile(fileKey,file);items.push({id:old?.id||crypto.randomUUID(),fileKey,versions:old?[...(old.versions||[]),{fileKey:old.fileKey||old.id,fileName:old.fileName,status:old.status}]:[],company:owner.id,title:old?.title||String(f.get('title')||'').trim()||file.name,fileName:file.name,status:'review_required',scope:old?.scope||String(f.get('scope')||'periodic'),period,date:new Date().toISOString().slice(0,10),size:file.size<1048576?Math.max(1,Math.round(file.size/1024))+' KB':(file.size/1048576).toFixed(1)+' MB',demo:false});}setDocs(all=>[...all.filter(d=>!items.some(x=>x.id===d.id)),...items]);setCompanies(all=>all.map(c=>c.id===owner.id?{...c,isDemo:false}:c));reopen(owner.id);event('Hujjat yuklandi',owner,files.map(x=>x.name).join(', '),'upload');setModal(null);setToast(`${files.length} ta fayl saqlandi.${ai.connection.connected&&aiAuto?' Avtomatik tekshiruv fonda boshlanadi.':''}`);}catch{setError('Saqlash amalga oshmadi. Ulanishni va bo‘sh xotirani tekshiring.');}finally{setBusy(false)}}
 function decide(status:string,reason=''){try{const d=activeDoc;const next={...transition(d,status,reason),reviewer:profile.fullName};setDocs(all=>all.map(x=>x.id===d.id?{...next,ai:x.ai}:x));event(status==='accepted'?'Hujjat qabul qilindi':'Tuzatish so‘raldi',company,`${d.title}${reason?' · '+reason:''}`,status==='accepted'?'check':'request');setModal(null);setToast('Qaror kompaniya tarixida saqlandi.');}catch(e:any){setError(e.message)}}
 function requestDocument(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const title=String(f.get('title')||'').trim();if(!title){setError('Hujjat nomini kiriting.');return;}setDocs(all=>[...all,{id:crypto.randomUUID(),company:company.id,title,fileName:'',period,status:'missing',scope:'periodic',date:new Date().toISOString().slice(0,10),size:'—',demo:false}]);reopen(company.id);setCompanies(all=>all.map(c=>c.id===company.id?{...c,isDemo:false}:c));event('Hujjat talabi yaratildi',company,title,'request');setModal(null);setToast('Talab saqlandi. Tashqi xabar yuborilmadi.');}
 async function download(d:any){try{const file=await getFile(d.fileKey||d.id,d.fileName);if(!file)throw Error();const url=URL.createObjectURL(file);const a=document.createElement('a');a.href=url;a.download=d.fileName;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}catch{setError('Asl fayl topilmadi.')}}
 function exportCSV(){const lines=[['Kompaniya','Hujjat','Davr','Holat'],...documentList.map(d=>[company.name,d.title,d.scope==='permanent'?'Doimiy':d.period,statuses[d.status]])];const url=URL.createObjectURL(new Blob(['\ufeff'+lines.map(r=>r.map(csvCell).join(',')).join('\r\n')],{type:'text/csv;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download=`${company.name}-${period}.csv`;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);setToast('Hujjatlar reyestri yuklab olindi.');}
 const shared=<>{savedError&&<div className="storage-error" role="alert">{savedError} <button onClick={retrySave}>Qayta saqlash</button><button onClick={()=>open({type:'backup'})}>Zaxira olish</button></div>}{toast&&<div className="toast" role="status"><Icon name="check"/>{toast}<button onClick={()=>setToast('')} aria-label="Xabarni yopish"><Icon name="close" size={16}/></button></div>}</>;
 if(!profile)return <div className="onboarding"><header className="onboarding-header"><Brand/><Button variant="outline" onClick={()=>open({type:'backup'})}><Icon name="upload"/>Zaxiradan tiklash</Button></header><main className="onboarding-main"><aside className="onboarding-intro"><span className="section-kicker">HISOBKOR.UZ BILAN ISHNI BOSHLASH</span><h1>Barcha kompaniyalaringiz.<br/>Bitta tartibli ish joyi.</h1><p>Hujjatlarni yig‘ing, tekshiring va har bir kompaniya bo‘yicha tarixni saqlang.</p><ol className="setup-steps"><li className="current"><span>1</span><div><strong>O‘zingizni tanishtiring</strong><p>Buxgalter ma’lumotlarini kiriting</p></div></li><li><span>2</span><div><strong>Kompaniya qo‘shing</strong><p>Xizmat ko‘rsatadigan tashkilotingiz</p></div></li><li><span>3</span><div><strong>Hujjatlar bilan ishlang</strong><p>Yuklang, tekshiring va tarixni kuzating</p></div></li></ol><div className="intro-footnote"><Icon name="folder"/>Hujjatlar har bir kompaniyada alohida saqlanadi.</div></aside><section className="onboarding-form panel"><div className="step-label">1 / 3 · BUXGALTER PROFILI</div><h2>Avval siz bilan tanishamiz</h2><p className="form-description">Hujjatlardagi qarorlar va tarixda ismingiz ko‘rinadi.</p>{ProfileForm({})}</section></main><Modal open={modal?.type==='backup'} onClose={()=>setModal(null)} title="Zaxira nusxalari" description="Ish joyingizni saqlash va ko‘chirish."><BackupPanel state={workspaceState} onRestore={restoreWorkspace}/></Modal><footer className="onboarding-footer">Hisobkor.uz · Buxgalterning kundalik ish joyi</footer>{shared}</div>;
 const sidebarContent=<><Brand/><div className="workspace-label"><span>Ish joyi</span><strong>{profile.workspace||'Mening ish joyim'}</strong></div><div className="sidebar-nav"><button className={!company?'nav-all selected':'nav-all'} onClick={()=>go('companies')}><Icon name="dashboard"/>Kompaniyalar</button><div className="sidebar-caption">TANLANGAN KOMPANIYA</div><DropdownMenu.Root modal={false}><DropdownMenu.Trigger asChild><button className="company-switcher selected" aria-label={company?`Kompaniyani almashtirish: ${company.name}`:'Kompaniya tanlash'}><Icon name="company"/><strong>{company?.name||'Kompaniyalar'}</strong><span className="switcher-count">{mine.length}</span><Icon name="down" size={16}/></button></DropdownMenu.Trigger><DropdownMenu.Portal><DropdownMenu.Content className="company-dropdown" align="start" sideOffset={8} collisionPadding={12} onCloseAutoFocus={e=>{if(modal)e.preventDefault()}}><DropdownMenu.Label className="company-dropdown-label">Kompaniya tanlang</DropdownMenu.Label><div className="company-dropdown-list"><DropdownMenu.RadioGroup value={company?.id||''} onValueChange={id=>go(`company/${id}/documents`)}>{(company?.isDemo?[...mine,company]:mine).map(c=><DropdownMenu.RadioItem className="company-dropdown-item" key={c.id} value={c.id}><Icon name="company" size={18}/><span>{c.name}{c.isDemo&&<small>Namuna kompaniya</small>}</span><DropdownMenu.ItemIndicator className="company-dropdown-check"><Icon name="check" size={16}/></DropdownMenu.ItemIndicator></DropdownMenu.RadioItem>)}</DropdownMenu.RadioGroup>{!mine.length&&!company&&<p className="company-dropdown-empty">Hali kompaniya qo‘shilmagan</p>}</div><DropdownMenu.Separator className="company-dropdown-separator"/><DropdownMenu.Item className="company-dropdown-item" onSelect={()=>go('companies')}><Icon name="dashboard"/>Barcha kompaniyalar</DropdownMenu.Item><DropdownMenu.Item className="company-dropdown-item" onSelect={()=>open({type:'company'})}><Icon name="plus"/>Kompaniya qo‘shish</DropdownMenu.Item></DropdownMenu.Content></DropdownMenu.Portal></DropdownMenu.Root></div>{company&&<div className="company-navigation"><div className="sidebar-caption">KOMPANIYA BO‘LIMLARI</div><nav>{tabs.map(([id,label,icon])=><button key={id} className={tab===id?'selected':''} onClick={()=>go(`company/${company.id}/${id}`)}><Icon name={icon}/>{label}</button>)}</nav></div>}<div className="sidebar-bottom"><Button variant="ghost" className="full-width" onClick={()=>open({type:'backup'})}><Icon name="download"/>Zaxira nusxalari</Button><div className="local-mode"><Icon name="shield" size={17}/><span>{session.storage==='server'?'Shaxsiy ish joyi':'Shu qurilmada'}</span><button aria-label="Saqlash haqida" onClick={()=>open({type:'help'})}><Icon name="help" size={16}/></button></div><button className="profile-button" onClick={()=>open({type:'profile'})}><span className="profile-avatar">{initials(profile.fullName)}</span><span><strong>{profile.fullName}</strong><small>Buxgalter</small></span><Icon name="settings" size={17}/></button>{session.mode==='production'&&<Button variant="ghost" className="full-width logout-button" onClick={signOut}><Icon name="logout"/>Chiqish</Button>}</div></>;
 return <div className="app"><a className="skip-link" href="#main-content" onClick={e=>{e.preventDefault();document.getElementById('main-content')?.focus()}}>Asosiy mazmunga o‘tish</a><aside className="sidebar desktop-sidebar">{sidebarContent}</aside><Dialog.Root open={mobileMenu} onOpenChange={setMobileMenu}><Dialog.Portal><Dialog.Overlay className="mobile-backdrop"/><Dialog.Content className="sidebar mobile-sidebar" aria-describedby={undefined}><Dialog.Title className="sr-only">Asosiy menyu</Dialog.Title><Dialog.Close asChild><button className="mobile-nav-close" aria-label="Menyuni yopish"><Icon name="close"/></button></Dialog.Close>{sidebarContent}</Dialog.Content></Dialog.Portal></Dialog.Root><div className="workspace"><header className="topbar"><button className="mobile-menu" aria-label="Menyu" aria-expanded={mobileMenu} onClick={()=>setMobileMenu(true)}><Icon name="menu"/></button><div className="breadcrumb"><button onClick={()=>go('companies')}>Kompaniyalar</button>{company&&<><span>/</span><strong>{company.name}</strong><span>/</span><span className="breadcrumb-page">{tabs.find(t=>t[0]===tab)?.[1]}</span></>}</div><div className="topbar-actions"><span className="save-indicator" role="status"><Icon name={saveStatus==='error'?'alert':saveStatus==='saving'?'clock':'check'} size={15}/>{saveStatus==='error'?'Saqlanmadi':saveStatus==='saving'?'Saqlanmoqda…':'Saqlandi'}</span><Button variant="ghost" size="icon" aria-label="Yordam" onClick={()=>open({type:'help'})}><Icon name="help"/></Button></div></header><main className="main-content" id="main-content" tabIndex={-1}>

{!company?<><div className="page-heading"><div><span className="section-kicker">SIZNING ISH JOYINGIZ</span><h1>Kompaniyalaringiz</h1><p>Har bir hisob, hujjat va qaror — o‘z joyida.</p></div><Button onClick={()=>open({type:'company'})}><Icon name="plus"/>Kompaniya qo‘shish</Button></div>{mine.length>0&&<div className="overview-stats"><div><Icon name="company"/><span>Kompaniyalar<strong>{mine.length}</strong></span></div><div><Icon name="document"/><span>Yuklangan hujjatlar<strong>{realDocs.filter(d=>d.status!=='missing').length}</strong></span></div><div><Icon name="review"/><span>Tekshirish kerak<strong>{realDocs.filter(d=>d.status==='review_required').length}</strong></span></div></div>}{mine.length===0?<section className="panel first-company"><div className="step-strip"><span><Icon name="check"/>Profil to‘ldirildi</span><span className="active">2. Kompaniya qo‘shish</span><span>3. Hujjatlar bilan ishlash</span></div><Empty title="Birinchi kompaniyangizni qo‘shing" text="Kompaniya nomi yetarli. Qolgan ma’lumotlarni keyin to‘ldirishingiz mumkin." action={<Button onClick={()=>open({type:'company'})}><Icon name="plus"/>Kompaniya qo‘shish</Button>}/></section>:<><div className="company-toolbar"><div className="search-field"><Icon name="search"/><Input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Kompaniya nomi yoki STIR" aria-label="Kompaniyalarni qidirish"/></div><span>{mine.length} ta kompaniya</span></div><div className="company-grid">{mine.filter(c=>`${c.name} ${c.stir}`.toLowerCase().includes(query.toLowerCase())).map(c=><CompanyCard key={c.id} c={c} docs={docs} onOpen={()=>go(`company/${c.id}/documents`)}/>)}</div>{!mine.some(c=>`${c.name} ${c.stir}`.toLowerCase().includes(query.toLowerCase()))&&<Empty title="Kompaniya topilmadi" text="Boshqa nom yoki STIR bilan qidiring."/>}</>}{examples.length>0&&<section className="example-section"><button className="example-toggle" onClick={()=>setShowExamples(!showExamples)} aria-expanded={showExamples}><Icon name="folder"/>Avvalgi namuna kompaniyalar ({examples.length})<Icon name="down"/></button>{showExamples&&<><p>Oldingi preview ma’lumotlari saqlangan. Bu kompaniyalar namuna uchun.</p><div className="company-grid">{examples.map(c=><CompanyCard key={c.id} c={c} docs={docs} onOpen={()=>go(`company/${c.id}/documents`)}/>)}</div></>}</section>}<div className="company-hint"><Icon name="shield"/><span>Har bir kompaniyaning hujjatlari, qarorlari va tarixi alohida.</span></div></>:<><button className="back-link" onClick={()=>go('companies')}><Icon name="arrow" size={16}/>Barcha kompaniyalar</button><div className="page-heading company-heading"><div className="company-title"><span className="company-mark large"><Icon name="company" size={28}/></span><div><h1>{tabs.find(t=>t[0]===tab)?.[1]}</h1><p>{company.name} · {company.legal||'MChJ'}{company.stir?` · STIR ${company.stir}`:''}{company.isDemo?' · Namuna kompaniya':''}</p></div></div>{['documents','archive'].includes(tab)&&<Button onClick={()=>open({type:'upload'})}><Icon name="upload"/>Hujjat yuklash</Button>}</div><nav className="company-tabs" aria-label="Kompaniya bo‘limlari">{tabs.map(([id,label,icon])=><button key={id} aria-current={tab===id?'page':undefined} className={tab===id?'active':''} onClick={()=>go(`company/${company.id}/${id}`)}><Icon name={icon} size={17}/>{label}</button>)}</nav>
{['documents','archive'].includes(tab)&&<><div className="section-heading"><div><h2>{tab==='archive'?'Kompaniya arxivi':month(period)}</h2><p>{tab==='archive'?'Ustav, guvohnoma, rekvizitlar va shartnomalar.':'Shu oyga tegishli hujjatlarni yuklang va tekshiring.'}</p></div>{tab==='documents'&&<div className="period-control"><Icon name="calendar"/><input type="month" aria-label="Hisobot davri" value={period} min="2000-01" max="2100-12" onChange={e=>{if(/^\d{4}-\d{2}$/.test(e.target.value)){setPeriod(e.target.value);setFilter('all')}}}/></div>}</div>{tab==='documents'&&current.length>0&&<div className="summary-strip">{[['all','Jami hujjatlar',stats.total,'document'],['review_required','Tekshirish kerak',stats.review,'search'],['accepted','Qabul qilingan',stats.accepted,'check'],['outstanding','Kutilmoqda',stats.missing,'clock']].map(([key,label,count,icon])=><button key={key} className={filter===key?'active':''} aria-pressed={filter===key} onClick={()=>setFilter(String(key))}><span>{label}<Icon name={icon} size={17}/></span><strong>{count}</strong></button>)}</div>}<AIBar ai={ai} docs={docs.filter(d=>d.company===company.id)}/><section className="panel documents-panel"><div className="document-toolbar"><div className="search-field"><Icon name="search"/><Input placeholder="Hujjat nomini qidirish" aria-label="Hujjat qidirish" value={query} onChange={e=>setQuery(e.target.value)}/></div><div className="toolbar-actions"><select value={filter} aria-label="Hujjat holati" onChange={e=>setFilter(e.target.value)}><option value="all">Barcha holatlar</option><option value="outstanding">Barcha kutilayotganlar</option>{Object.entries(statuses).map(([key,value]:any)=><option key={key} value={key}>{value}</option>)}</select>{tab==='documents'&&<Button variant="outline" onClick={()=>open({type:'request'})}><Icon name="plus"/>Hujjat so‘rash</Button>}<Button variant="outline" size="icon" disabled={!documentList.length} onClick={exportCSV} aria-label="Hujjatlar reyestrini yuklab olish" title="Reyestrni yuklab olish"><Icon name="download"/></Button></div></div>{visible.length?<div className="table-wrap"><table><caption className="sr-only">{company.name} hujjatlari</caption><thead><tr><th scope="col">Hujjat</th><th scope="col">Sana</th><th scope="col">Holat</th><th scope="col">Avto tekshiruv</th><th scope="col"><span className="sr-only">Amal</span></th></tr></thead><tbody>{pageDocs.map(d=><tr key={d.id}><td><button className="document-name" onClick={()=>showDocument(d)}><span className="file-icon"><Icon name={d.status==='missing'?'request':'document'}/></span><span><strong>{documentName(d)}</strong><small>{d.demo?'Namuna hujjati':`${d.fileName?.split('.').pop()?.toUpperCase()||'Fayl'} · ${d.size}`}{d.reason?` · ${d.reason}`:''}</small></span></button></td><td className="date-cell">{d.date?d.date.split('-').reverse().join('.'):'—'}</td><td><Badge status={d.status}/></td><td className="ai-cell">{eligible(d)?<AIBadge doc={d}/>:<span className="muted">{d.demo?'Namuna':'Fayl kutilmoqda'}</span>}</td><td><Button variant="ghost" onClick={()=>showDocument(d)}>{d.status==='missing'?'Yuklash':d.status==='review_required'?'Tekshirish':'Ochish'}<Icon name="arrow" size={16}/></Button></td></tr>)}</tbody></table></div>:<Empty title={documentList.length?'Hujjat topilmadi':tab==='archive'?'Doimiy hujjatlarni shu yerda saqlang':'Birinchi hujjatni yuklang'} text={documentList.length?'Qidiruvni yoki holat filtrini o‘zgartiring.':tab==='archive'?'Bu fayllar oy almashganda ham kompaniya ichida qoladi.':'PDF, rasm, Excel yoki CSV fayl qo‘shishingiz mumkin.'} action={!documentList.length&&<Button onClick={()=>open({type:'upload'})}><Icon name="upload"/>Hujjat yuklash</Button>}/>}<div className="table-footer"><span>{visible.length?`${(safePage-1)*pageSize+1}–${Math.min(safePage*pageSize,visible.length)} / ${visible.length} ta hujjat`:'0 ta hujjat'}</span><div><Button variant="ghost" disabled={safePage<=1} onClick={()=>setPage(safePage-1)} aria-label="Oldingi sahifa"><Icon name="arrow" className="flip" size={16}/></Button><span>{safePage} / {totalPages}</span><Button variant="ghost" disabled={safePage>=totalPages} onClick={()=>setPage(safePage+1)} aria-label="Keyingi sahifa"><Icon name="arrow" size={16}/></Button></div></div></section>{docs.some(d=>d.company===company.id&&d.demo)&&<label className="sample-switch"><input type="checkbox" checked={showSampleDocs} onChange={e=>setShowSampleDocs(e.target.checked)}/>Avvalgi namuna hujjatlarini ko‘rsatish</label>}{tab==='documents'&&current.length>0&&<div className="period-footer"><span>{closed.includes(`${company.id}:${period}`)?'Bu davr yopilgan.':stats.ready?'Barcha hujjatlar qabul qilingan. Davrni yopishingiz mumkin.':'Davrni yopish uchun barcha talablarni yakunlang.'}</span><Button variant="outline" disabled={!stats.ready||closed.includes(`${company.id}:${period}`)} onClick={()=>open({type:'close'})}>{closed.includes(`${company.id}:${period}`)?'Davr yopilgan':'Davrni yopish'}</Button></div>}</>}
{tab==='history'&&<section className="history-section"><div className="section-heading"><div><h2>Kompaniya tarixi</h2><p>Kim, qachon va qanday amal bajarganini kuzating.</p></div></div><div className="panel">{companyHistory(activity,company).length?companyHistory(activity,company).map((a:any,i:number)=><div className="history-item" key={a.id||i}><span className="history-symbol"><Icon name={a.type||'clock'}/></span><div><h3>{a.title}</h3><p>{a.detail?.replace(company.name+' · ','')}</p><small>{a.actor||'Avvalgi preview'}</small></div><time>{a.at?new Date(a.at).toLocaleDateString('uz-UZ'):''}<span>{a.time}</span></time></div>):<Empty title="Hali amallar yo‘q" text="Hujjat yuklash va qabul qilish amallari shu yerda ko‘rinadi."/>}</div></section>}
{tab==='info'&&<section className="panel company-info"><div className="section-heading"><div><h2>Kompaniya ma’lumotlari</h2><p>Rekvizitlar va hujjatlarni topshiruvchi vakil.</p></div><Button variant="outline" onClick={()=>open({type:'edit-company'})}>Tahrirlash</Button></div><dl>{[['Kompaniya nomi',`${company.name} ${company.legal||''}`],['STIR',company.stir||'Kiritilmagan'],['Mas’ul buxgalter',company.isDemo?company.owner:profile.fullName],['Mijoz vakili',company.contact||'Kiritilmagan'],['Telefon raqami',company.phone||'Kiritilmagan'],['Asosiy valyuta','UZS — O‘zbekiston so‘mi']].map(([label,value])=><div key={label}><dt>{label}</dt><dd>{value}</dd></div>)}</dl></section>}
</>}
<footer className="workspace-footer"><span>Hisobkor.uz · Buxgalterning ish joyi</span><span>{session.storage==='server'?'Ma’lumotlar serverda saqlanadi':'Ma’lumotlar shu qurilmada saqlanadi'}</span></footer></main></div>
<Modal open={modal?.type==='backup'} onClose={()=>setModal(null)} title="Zaxira nusxalari" description="Ish joyingizni saqlash va ko‘chirish."><BackupPanel state={workspaceState} onRestore={restoreWorkspace}/></Modal>

<Modal open={modal?.type==='profile'} onClose={()=>setModal(null)} title="Buxgalter profili" description="Qarorlar va tarixda ko‘rinadigan ma’lumotlaringiz.">{ProfileForm({editing:true})}</Modal>
<Modal open={modal?.type==='company'} onClose={()=>setModal(null)} title="Kompaniya qo‘shish" description="Avval nomini kiriting. Boshqa ma’lumotlar ixtiyoriy."><form onSubmit={addCompany}><CompanyFields/>{error&&<p role="alert" className="form-error">{error}</p>}<div className="modal-footer"><Button variant="outline" type="button" onClick={()=>setModal(null)}>Bekor qilish</Button><Button type="submit">Kompaniyani qo‘shish<Icon name="arrow"/></Button></div></form></Modal>
<Modal open={modal?.type==='edit-company'} onClose={()=>setModal(null)} title="Kompaniya ma’lumotlari" description={company?.name}><form onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);const name=String(f.get('name')).trim();const stir=String(f.get('stir')||'').trim();if(!name){setError('Kompaniya nomini kiriting.');return;}if(stir&&companies.some(c=>c.id!==company.id&&c.stir===stir)){setError('Bu STIR boshqa kompaniyada mavjud.');return;}const changes={name,stir,legal:String(f.get('legal')),contact:String(f.get('contact')||'').trim(),phone:String(f.get('phone')||'').trim()};setCompanies(all=>all.map(c=>c.id===company.id?{...c,...changes}:c));event('Kompaniya ma’lumotlari yangilandi',company,profile.fullName,'company');setModal(null);setToast('Ma’lumotlar saqlandi.');}}><CompanyFields company={company}/>{error&&<p role="alert" className="form-error">{error}</p>}<div className="modal-footer"><Button type="submit">Saqlash</Button></div></form></Modal>
<Modal open={modal?.type==='upload'} onClose={()=>!busy&&setModal(null)} title="Hujjat yuklash" description={`${company?.name||''} · ${tab==='archive'?'Doimiy arxiv':month(period)}`}>
{modal?.type==='upload'&&<UploadForm onSubmit={upload} busy={busy} error={error} archive={tab==='archive'} target={modal.target}/>}</Modal>
<Modal open={modal?.type==='request'} onClose={()=>setModal(null)} title="Hujjat so‘rash" description={`${company?.name||''} · ${month(period)}`}><form onSubmit={requestDocument}><label>Qanday hujjat kerak?<Input required name="title" maxLength={120} placeholder="Masalan, sentabr oyi bank ko‘chirmasi"/></label><div className="neutral-note"><Icon name="help"/>Talab kompaniya ro‘yxatiga qo‘shiladi. Hozircha mijozga avtomatik xabar yuborilmaydi.</div>{error&&<p className="form-error" role="alert">{error}</p>}<div className="modal-footer"><Button type="submit">Talab qo‘shish</Button></div></form></Modal>
<Modal open={modal?.type==='document'} onClose={()=>setModal(null)} title={activeDoc?documentName(activeDoc):'Hujjat'} description={company?.name} wide>{activeDoc&&<div className="document-detail"><section className="file-preview">{preview?(activeDoc.fileName.toLowerCase().endsWith('.pdf')?<iframe title="PDF hujjat" src={preview+'#navpanes=0&view=FitH'}/>:<img src={preview} alt={activeDoc.title}/>):<div className="file-placeholder"><Icon name="document" size={38}/><h3>{activeDoc.demo?'Namuna hujjati':'Fayl saqlangan'}</h3><p>{activeDoc.demo?'Avvalgi preview’dan qolgan namuna. Asl fayl biriktirilmagan.':'Excel yoki CSV faylini yuklab olib ochishingiz mumkin.'}</p><span>{activeDoc.fileName}</span></div>}</section><section className="document-details"><Badge status={activeDoc.status}/><div className="document-meta"><span className="file-icon"><Icon name="document"/></span><div><strong>{activeDoc.fileName}</strong><p>{activeDoc.date?activeDoc.date.split('-').reverse().join('.'):'Sana ko‘rsatilmagan'} · {activeDoc.size}</p></div></div><AIReview doc={activeDoc} docs={docs} company={company} ai={ai} onRelated={showDocument}/><dl><dt>Kompaniya</dt><dd>{company?.name}</dd><dt>Bo‘lim</dt><dd>{activeDoc.scope==='permanent'?'Doimiy hujjatlar':month(activeDoc.period)}</dd><dt>Fayl hajmi</dt><dd>{activeDoc.size}</dd>{activeDoc.reviewer&&<><dt>Qabul qilgan buxgalter</dt><dd>{activeDoc.reviewer}</dd></>}</dl>{activeDoc.reason&&<div className="neutral-note">{activeDoc.reason}</div>}{activeDoc.status==='review_required'&&<form onSubmit={e=>{e.preventDefault();decide('correction_requested',String(new FormData(e.currentTarget).get('reason')||''))}}><p className="decision-help">Hujjatni tekshiring va qaroringizni belgilang.</p><Button type="button" className="full-width" onClick={()=>decide('accepted')}><Icon name="check"/>Qabul qilish</Button><label className="reason-label">Tuzatish kerak bo‘lsa<textarea name="reason" rows={3} placeholder="Sababini yozing"/></label><Button type="submit" variant="outline" className="full-width">Tuzatishga qaytarish</Button></form>}{error&&<p className="form-error" role="alert">{error}</p>}{activeDoc.status==='correction_requested'&&<Button className="full-width" onClick={()=>open({type:'upload',target:activeDoc})}>Yangi nusxa yuklash</Button>}{!activeDoc.demo&&<Button variant="ghost" className="full-width download-original" onClick={()=>download(activeDoc)}><Icon name="download"/>Asl faylni yuklab olish</Button>}</section></div>}</Modal>
<Modal open={modal?.type==='close'} onClose={()=>setModal(null)} title="Davrni yopish" description={`${company?.name||''} · ${month(period)}`}><p>Barcha talablar bajarilganini tekshirdingizmi? Yangi hujjat yoki talab qo‘shilsa, davr qayta ochiladi.</p><div className="modal-footer"><Button variant="outline" onClick={()=>setModal(null)}>Bekor qilish</Button><Button onClick={()=>{if(!stats.ready){setError('Hali bajarilmagan talablar bor.');return;}setClosed(a=>[...a,`${company.id}:${period}`]);event('Hisobot davri yopildi',company,month(period),'check');setModal(null);setToast('Davr yopildi.');}}>Davrni yopish</Button></div>{error&&<p role="alert">{error}</p>}</Modal>
<Modal open={modal?.type==='help'} onClose={()=>setModal(null)} title="Hisobkor.uz bilan ishlash" description="Uchta oddiy qadam."><div className="help-steps"><p><b>1. Profilingizni to‘ldiring.</b> Qarorlar sizning ismingiz bilan saqlanadi.</p><p><b>2. Kompaniya qo‘shing.</b> Har bir tashkilotning hujjatlari alohida bo‘ladi.</p><p><b>3. Kompaniya ichida ishlang.</b> Hujjat yuklang, qabul qiling yoki tuzatishni so‘rang. Amallar “Tarix” bo‘limida ko‘rinadi.</p><div className="neutral-note">{session.storage==='server'?'Bu shaxsiy buxgalter kabineti. Ma’lumotlar serverda saqlanadi.':'Bu lokal ish joyi. Mavjud bo‘lsa, avtomatik tekshiruv fayllarga yordamchi xulosa beradi.'} Schyot-faktura va shartnoma rekvizitlari kompaniya ichida solishtiriladi. Didox va mijozga xabar yuborish ulanmagan. Brauzer ma’lumotlari o‘chirilsa, saqlangan fayllar ham yo‘qoladi.</div></div></Modal>
{shared}</div>;
}
function CompanyCard({c,docs,onOpen}:any){const all=docs.filter((d:any)=>d.company===c.id&&(c.isDemo||!d.demo));const n=all.filter((d:any)=>d.status==='review_required').length;const count=all.filter((d:any)=>d.status!=='missing').length;return <button className="company-card" onClick={onOpen}><span className="company-mark"><Icon name="company" size={23}/></span><span className="company-card-info"><strong>{c.name}</strong><span>{c.legal||'MChJ'}{c.stir?` · STIR ${c.stir}`:''}</span></span><span className="company-card-count"><Icon name="document" size={16}/>{count} ta hujjat</span><span className={n?'review-count':'company-card-state'}>{n?`${n} ta tekshirish kerak`:'Ish joyini ochish'}</span><Icon name="arrow" size={17}/></button>}

function CompanyFields({company}:any){return <><label>Kompaniya nomi <span aria-hidden="true">*</span><Input required name="name" maxLength={100} placeholder="Masalan, Atlas Savdo" defaultValue={company?.name||''}/></label><div className="form-grid"><label>Tashkiliy shakli<select name="legal" defaultValue={company?.legal||'MChJ'}><option>MChJ</option><option>YTT</option><option>AJ</option><option>Boshqa</option></select></label><label>STIR <span className="optional">Ixtiyoriy</span><Input name="stir" inputMode="numeric" pattern="[0-9]{9}" maxLength={9} placeholder="9 ta raqam" defaultValue={company?.stir||''}/></label></div><details className="extra-fields" open={!!company}><summary>Mijoz vakili va aloqa ma’lumotlari</summary><label>Mijoz vakili<Input name="contact" maxLength={100} placeholder="Ism va familiya" defaultValue={company?.contact||''}/></label><label>Telefon raqami<Input name="phone" type="tel" maxLength={20} placeholder="+998" defaultValue={company?.phone||''}/></label></details></>}
function UploadForm({onSubmit,busy,error,archive,target}:any){const [files,setFiles]=useState<File[]>([]);return <form onSubmit={e=>onSubmit(e,files)}>{target?<div className="neutral-note">Talab: {target.title}</div>:<><label>Hujjat nomi <span className="optional">Ixtiyoriy</span><Input name="title" maxLength={120} placeholder="Kiritmasangiz, fayl nomi saqlanadi"/></label><label>Saqlash joyi<select name="scope" defaultValue={archive?'permanent':'periodic'}><option value="periodic">Oylik hujjatlar</option><option value="permanent">Doimiy hujjatlar</option></select></label></>}<label className="drop-zone" onDragOver={e=>e.preventDefault()} onDrop={e=>{e.preventDefault();setFiles(Array.from(e.dataTransfer.files))}}><span className="upload-circle"><Icon name="upload" size={24}/></span><strong>{files.length?`${files.length} ta fayl tanlandi`:'Fayl tanlang yoki shu yerga tashlang'}</strong><span>PDF, JPG, PNG, XLSX yoki CSV</span><small>Har bir fayl 25 MBgacha</small><input type="file" aria-label="Fayl tanlash" accept=".pdf,.jpg,.jpeg,.png,.xlsx,.csv" multiple={!target} onChange={e=>setFiles(Array.from(e.target.files||[]))}/></label>{files.length>0&&<ul className="file-list">{files.map((f,i)=><li key={i}><Icon name="document" size={16}/><span>{f.name}</span><small>{Math.max(1,Math.round(f.size/1024))} KB</small></li>)}</ul>}{error&&<p className="form-error" role="alert">{error}</p>}<div className="modal-footer"><span className="upload-note">Avtomatik qabul qilinmaydi</span><Button type="submit" disabled={busy}><Icon name="upload"/>{busy?'Saqlanmoqda…':'Yuklash'}</Button></div></form>}
createRoot(document.getElementById('root')!).render(<ErrorBoundary><Runtime>{({initial,session,logout}:any)=><App saved={initial} session={session} logout={logout}/>}</Runtime></ErrorBoundary>);
